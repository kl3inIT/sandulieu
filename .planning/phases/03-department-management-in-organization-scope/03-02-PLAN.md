---
phase: 03-department-management-in-organization-scope
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx
  - features/departments/components/DepartmentListFilters.tsx
  - features/departments/components/DepartmentStatusBadge.tsx
  - features/departments/components/DepartmentListTable.tsx
  - features/departments/components/DepartmentRowActions.tsx
autonomous: true
requirements:
  - DEPT-01
  - DEPT-02
  - DEPT-03
  - DEPT-04
  - DEPT-11
must_haves:
  truths:
    - "User can browse departments in an organization-scoped enterprise table rather than proof cards."
    - "User can narrow the department list by code/name search and status while parent organization scope remains explicit."
    - "User can sort and paginate the table using backend-shaped organization-qualified list behavior."
    - "Each row exposes quick actions for detail and edit immediately, with a reserved action slot for the shared delete flow added in 03-05."
  artifacts:
    - path: "app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx"
      provides: "Organization-scoped department list route orchestration"
      contains: "URL-owned search/filter/sort/page wiring"
    - path: "features/departments/components/DepartmentListTable.tsx"
      provides: "Enterprise department table"
      contains: "code, name, status"
    - path: "features/departments/components/DepartmentRowActions.tsx"
      provides: "Per-row nested quick actions"
      contains: "detail and edit entrypoints, plus reserved slot for shared delete flow"
  key_links:
    - from: "app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx"
      to: "features/departments/department.query-hooks.ts"
      via: "useDepartmentListQuery"
      pattern: "useDepartmentListQuery"
    - from: "app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx"
      to: "features/directory/shared"
      via: "parseDirectoryListState/buildDirectoryTableState/toDirectoryListQuery with organization scope"
      pattern: "(parseDirectoryListState|buildDirectoryTableState|toDirectoryListQuery|createOrganizationScope)"
    - from: "features/departments/components/DepartmentListTable.tsx"
      to: "features/departments/components/DepartmentStatusBadge.tsx"
      via: "status cell rendering"
      pattern: "DepartmentStatusBadge"
---

<objective>
Replace the proof nested departments page with a production-shaped organization-scoped enterprise table.

Purpose: Deliver the browse/search/filter/sort/paginate surface required by Phase 3 while preserving the Phase 1 URL-state and parent-scope foundations and the Phase 2 organization nested routing pattern.
Output: A production department list route and feature components for filters, status badges, enterprise table layout, and nested row actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\DTH\sandulieu\.planning\PROJECT.md
@D:\DTH\sandulieu\.planning\ROADMAP.md
@D:\DTH\sandulieu\.planning\REQUIREMENTS.md
@D:\DTH\sandulieu\.planning\phases\01-directory-foundations-contracts\RESEARCH.md
@D:\DTH\sandulieu\.planning\phases\02-organization-management\RESEARCH.md
@D:\DTH\sandulieu\.planning\phases\02-organization-management\02-organization-management-04-SUMMARY.md
@D:\DTH\sandulieu\CLAUDE.md
@D:\DTH\sandulieu\app\(admin-portal)\admin\organizations\[organizationId]\departments\page.tsx
@D:\DTH\sandulieu\features\directory\shared\directory-list-state.ts
@D:\DTH\sandulieu\features\directory\shared\directory-table-state.ts
@D:\DTH\sandulieu\features\directory\shared\directory-scope.ts
@D:\DTH\sandulieu\features\departments\department.query-hooks.ts

<interfaces>
From features/departments/department.query-hooks.ts:
```ts
export function useDepartmentListQuery(
  organizationId: string,
  params: DepartmentListParams
) {
  return useQuery(departmentListQueryOptions(organizationId, params));
}
```

From the existing route proof:

```ts
const organizationScope = createOrganizationScope(params.organizationId);
const normalizedState = parseDirectoryListState(searchParams, options);
const tableState = buildDirectoryTableState(normalizedState);
const query = toDirectoryListQuery(normalizedState, {
  organizationId: organizationScope.organizationId,
});
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract department list filters, badges, and enterprise table components</name>
  <files>features/departments/components/DepartmentListFilters.tsx, features/departments/components/DepartmentStatusBadge.tsx, features/departments/components/DepartmentListTable.tsx</files>
  <behavior>
    - Test 1: table renders code and name as the primary scan columns.
    - Test 2: status is shown consistently as a badge and can be consumed by filter controls.
    - Test 3: empty/loading/error states render as enterprise admin states instead of proof cards.
    - Test 4: organization scope remains visible on the screen so the parent context is not lost while browsing departments.
  </behavior>
  <action>Build department-specific feature components for the list experience. Use shadcn-based Table/Card/Badge/Input composition without modifying base UI primitives. The table must be enterprise-oriented, not a proof card list; code and name must be the primary scan columns; status must have a dedicated badge column and filter control; and the organization-qualified parent scope must remain visible in the page header or filters area so DEPT-03 is satisfied through explicit nested scope plus status filtering rather than a separate organization picker.</action>
  <verify>
    <automated>pnpm exec eslint features/departments/components/DepartmentListFilters.tsx features/departments/components/DepartmentStatusBadge.tsx features/departments/components/DepartmentListTable.tsx</automated>
  </verify>
  <done>Reusable department list components exist and are ready for route orchestration, with enterprise table behavior and visible organization context instead of proof cards.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the proof department list route with URL-driven organization-scoped browsing</name>
  <files>app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx, features/departments/components/DepartmentRowActions.tsx</files>
  <behavior>
    - Test 1: search query updates the list by code/name through URL-owned state.
    - Test 2: status filter and sorting flow through the normalized directory list state under the current organization route.
    - Test 3: pagination reflects backend-shaped page state rather than client-only transforms.
    - Test 4: each row exposes quick action entrypoints for detail and edit, while preserving a clear reserved slot for the shared delete flow introduced in 03-05.
  </behavior>
  <action>Rewrite the nested departments route so it becomes the production department browse screen. Keep URL parsing/serialization as the owner of search, status, sort, and pagination; preserve the route’s `organizationId` as explicit parent scope in every query; render the new feature components for filters, table, status badges, and row actions; and add links to create, detail, and edit department routes inside the organization path. Do not reintroduce proof-only card mapping or any global unscoped department behavior.</action>
  <verify>
    <automated>pnpm exec eslint app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx features/departments/components/DepartmentRowActions.tsx</automated>
  </verify>
  <done>The nested departments page is now a production-style enterprise list with URL-owned controls, visible organization context, and dedicated-route navigation instead of proof-only cards.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                    | Description                                                                  |
| ------------------------------------------- | ---------------------------------------------------------------------------- |
| URL search params → nested department route | Untrusted query-string values drive search, filters, sorting, and pagination |
| route param → department query scope        | Untrusted `organizationId` defines parent scope for all department reads     |
| row actions → nested route navigation       | User-triggered actions determine detail, edit, and destructive targets       |

## STRIDE Threat Register

| Threat ID | Category | Component                              | Disposition | Mitigation Plan                                                                                                                       |
| --------- | -------- | -------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-06   | T        | nested query-param parsing             | mitigate    | Normalize all search/filter/sort/page inputs through shared directory-state helpers before building query params                      |
| T-03-07   | I        | department list table                  | mitigate    | Show only department summary fields required for admin browsing; keep member summary and detail-only content off the list screen      |
| T-03-08   | D        | enterprise table controls              | mitigate    | Use backend-shaped manual pagination/sorting state from shared helpers and avoid client-only list transforms that bypass parent scope |
| T-03-09   | E        | organization-scoped row action routing | mitigate    | Build nested detail/edit/delete routes from stable `organizationId` + `department.id` only                                            |

</threat_model>

<verification>
- `pnpm exec eslint app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx features/departments/components/DepartmentListFilters.tsx features/departments/components/DepartmentStatusBadge.tsx features/departments/components/DepartmentListTable.tsx features/departments/components/DepartmentRowActions.tsx`
- `pnpm build`
- Manual browser check on `/admin/organizations/<organizationId>/departments` confirms:
  - enterprise table layout instead of proof cards
  - search by code/name
  - status filter
  - sortable columns
  - paginated results
  - visible organization parent context
  - links to nested detail/edit/new routes
</verification>

<success_criteria>

- DEPT-01 through DEPT-04 are visibly satisfied on the organization-scoped departments route.
- The page preserves parent organization visibility while browsing and filtering.
- Code/name/status are the primary scan data.
- The list uses shared URL-state and parent-scope foundations rather than ad hoc local table state.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-department-management-in-organization-scope/03-department-management-in-organization-scope-02-SUMMARY.md`
</output>
