---
phase: 03-department-management-in-organization-scope
plan: 05
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-04
files_modified:
  - features/departments/components/DepartmentDeleteDialog.tsx
  - features/departments/components/DepartmentRowActions.tsx
  - app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx
autonomous: true
requirements:
  - DEPT-08
  - DEPT-09
must_haves:
  truths:
    - "User can delete a department only through a strict guarded soft-delete flow."
    - "Delete can be triggered consistently from both the nested list and detail surfaces."
    - "When members exist, the UI explains why delete is blocked and where to go next."
    - "Blocked delete preserves organization and department visibility by linking directly to the nested members surface."
  artifacts:
    - path: "features/departments/components/DepartmentDeleteDialog.tsx"
      provides: "Shared guarded delete UI"
      contains: "allowed and blocked states"
    - path: "features/departments/components/DepartmentRowActions.tsx"
      provides: "List-row entrypoint for the shared delete flow"
      contains: "DepartmentDeleteDialog"
    - path: "app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx"
      provides: "Detail-page entrypoint for the shared delete flow"
      contains: "delete action using DepartmentDeleteDialog"
  key_links:
    - from: "features/departments/components/DepartmentDeleteDialog.tsx"
      to: "features/departments/department.query-hooks.ts"
      via: "useDepartmentDeleteGuardQuery and useDeleteDepartmentMutation"
      pattern: "(useDepartmentDeleteGuardQuery|useDeleteDepartmentMutation)"
    - from: "features/departments/components/DepartmentDeleteDialog.tsx"
      to: "/admin/organizations/[organizationId]/departments/[departmentId]/members"
      via: "blocked-state management CTA"
      pattern: "members"
    - from: "features/departments/components/DepartmentRowActions.tsx"
      to: "features/departments/components/DepartmentDeleteDialog.tsx"
      via: "shared destructive flow from list rows"
      pattern: "DepartmentDeleteDialog"
---

<objective>
Finish the department vertical slice with a shared guarded delete flow reused by list and detail surfaces.

Purpose: Satisfy the remaining destructive lifecycle outcomes so departments can be managed end to end without breaking nested hierarchy rules or losing parent visibility when deletion is blocked.
Output: Shared strict delete dialog used from both list and detail entrypoints.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\DTH\sandulieu\.planning\PROJECT.md
@D:\DTH\sandulieu\.planning\ROADMAP.md
@D:\DTH\sandulieu\.planning\REQUIREMENTS.md
@D:\DTH\sandulieu\.planning\phases\02-organization-management\02-organization-management-04-SUMMARY.md
@D:\DTH\sandulieu\CLAUDE.md
@D:\DTH\sandulieu\features\departments\department.query-hooks.ts
@D:\DTH\sandulieu\features\departments\components\DepartmentRowActions.tsx
@D:\DTH\sandulieu\app\(admin-portal)\admin\organizations\[organizationId]\departments\[departmentId]\page.tsx

<interfaces>
Delete-guard contract expected from 03-01:
```ts
type DepartmentDeleteGuard = {
  allowed: boolean;
  dependentMemberCount: number;
  dependentMemberNames?: string[];
  messageKey: "hasDependentMembers" | "deletable";
};
```

Shared nested members route for blocked-state CTA:

```ts
/admin/aagiinnoorstz /
  [organizationId] /
  departments /
  [departmentId] /
  members;
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the shared guarded soft-delete dialog for departments</name>
  <files>features/departments/components/DepartmentDeleteDialog.tsx</files>
  <behavior>
    - Test 1: delete dialog renders a deletable state when no dependent members exist.
    - Test 2: delete dialog renders a blocked state when member records exist in the same organizationId + departmentId scope.
    - Test 3: blocked state explains the dependency reason and next step in Vietnamese.
    - Test 4: blocked state links directly to the nested members route for that department.
  </behavior>
  <action>Implement the shared delete experience for departments using a strict shadcn AlertDialog-style destructive flow, not a permissive inline action. The dialog must render two distinct states: deletable and blocked. In the blocked state, explain in Vietnamese that the department still has dependent members, state the dependency count and preview names if the guard contract provides them cleanly, and direct the user to manage members before retrying deletion. Keep the dialog reusable so list and detail surfaces cannot drift into different delete behavior.</action>
  <verify>
    <automated>pnpm exec eslint features/departments/components/DepartmentDeleteDialog.tsx</automated>
  </verify>
  <done>The shared department delete dialog exists, supports allowed and blocked states, and provides a route-safe next-step CTA to the nested members surface.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the shared delete flow into the nested list and detail surfaces</name>
  <files>features/departments/components/DepartmentRowActions.tsx, app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx</files>
  <behavior>
    - Test 1: delete from detail or list opens the same strict confirmation flow.
    - Test 2: if no dependent members exist, user can confirm soft-delete and the UI navigates safely afterward.
    - Test 3: if members exist, destructive confirmation is blocked and the UI explains the reason plus next step.
    - Test 4: blocked state keeps hierarchy visibility by linking to the current department's nested members page.
  </behavior>
  <action>Reuse `DepartmentDeleteDialog` from both the list row actions and the detail page so DEPT-08 and DEPT-09 are satisfied consistently. When deletion succeeds, invalidate organization-qualified department list/detail state and navigate safely back to the scoped departments list if the user deleted from detail. Do not implement one-off destructive logic separately in row actions or the page.</action>
  <verify>
    <automated>pnpm exec eslint features/departments/components/DepartmentRowActions.tsx app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx</automated>
  </verify>
  <done>Department delete is only available through one shared guarded soft-delete flow, and blocked deletes clearly explain member dependencies plus the next action.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                             | Description                                                                       |
| ------------------------------------ | --------------------------------------------------------------------------------- |
| delete dialog → mutation execution   | A destructive user action crosses into irreversible state change here             |
| guard result → blocked messaging/CTA | Dependency results must accurately shape which actions remain allowed             |
| nested delete success → navigation   | Post-delete navigation must remain inside the correct parent organization context |

## STRIDE Threat Register

| Threat ID | Category | Component                             | Disposition | Mitigation Plan                                                                                              |
| --------- | -------- | ------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------ |
| T-03-18   | T        | delete dialog action path             | mitigate    | Gate the destructive confirm button behind the guard result and never allow confirm when `allowed=false`     |
| T-03-19   | R        | blocked delete messaging              | mitigate    | Use structured guard payloads with explicit reason keys, dependency counts, and stable CTA generation        |
| T-03-20   | D        | shared delete flow across list/detail | mitigate    | Centralize the delete dialog component so list and detail do not drift into conflicting destructive behavior |
| T-03-21   | E        | navigation to members surface         | mitigate    | Generate the management CTA from stable `organizationId` + `departmentId` route identity only                |

</threat_model>

<verification>
- `pnpm exec eslint features/departments/components/DepartmentDeleteDialog.tsx features/departments/components/DepartmentRowActions.tsx app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx`
- `pnpm build`
- Manual browser checks:
  - trigger delete from both the list row actions and detail page
  - confirm the deletable state allows soft-delete and safe navigation afterward
  - confirm the blocked state explains member dependencies in Vietnamese and links to `/admin/organizations/<organizationId>/departments/<departmentId>/members`
</verification>

<success_criteria>

- DEPT-08 and DEPT-09 are fully satisfied with one shared guarded delete flow.
- List and detail surfaces share one consistent destructive behavior pattern.
- Blocked delete keeps both parent organization and current department context visible through the next-step route.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-department-management-in-organization-scope/03-department-management-in-organization-scope-05-SUMMARY.md`
</output>
