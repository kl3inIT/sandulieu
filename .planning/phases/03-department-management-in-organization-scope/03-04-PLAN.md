---
phase: 03-department-management-in-organization-scope
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx
  - features/departments/components/DepartmentDetailSummary.tsx
  - features/departments/components/DepartmentMemberSummary.tsx
autonomous: true
requirements:
  - DEPT-06
  - DEPT-11
must_haves:
  truths:
    - "User can open a dedicated department detail page with parent organization context and current status."
    - "User can see stable department identity, business code, display name, and any contract-backed metadata on the detail page."
    - "User can see related member summary for the current organizationId + departmentId scope."
    - "User can navigate from department detail back to departments list, edit, and the related members surface without losing hierarchy context."
  artifacts:
    - path: "app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx"
      provides: "Department detail route"
      contains: "summary, parent context, member summary, back/edit actions"
    - path: "features/departments/components/DepartmentDetailSummary.tsx"
      provides: "Department summary block"
      contains: "id, code, name, status, organization context"
    - path: "features/departments/components/DepartmentMemberSummary.tsx"
      provides: "Related member summary"
      contains: "member count and nested route CTA"
  key_links:
    - from: "app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx"
      to: "features/departments/department.query-hooks.ts"
      via: "useDepartmentDetailQuery"
      pattern: "useDepartmentDetailQuery"
    - from: "features/departments/components/DepartmentMemberSummary.tsx"
      to: "features/departments/department.query-hooks.ts"
      via: "member-summary data produced by the department detail query under organizationId + departmentId scope"
      pattern: "useDepartmentDetailQuery"
    - from: "app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx"
      to: "/admin/organizations/[organizationId]/departments/[departmentId]/members"
      via: "nested members CTA"
      pattern: "members"
---

<objective>
Add the department detail route with parent organization visibility and related member summary.

Purpose: Satisfy the summary-oriented detail outcome for Phase 3 so department management remains nested, intelligible, and connected to the next hierarchy level.
Output: Department detail page plus summary components for parent context and related members.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@D:\DTH\sandulieu\.planning\PROJECT.md
@D:\DTH\sandulieu\.planning\ROADMAP.md
@D:\DTH\sandulieu\.planning\REQUIREMENTS.md
@D:\DTH\sandulieu\.planning\phases\02-organization-management\RESEARCH.md
@D:\DTH\sandulieu\.planning\phases\02-organization-management\02-organization-management-04-SUMMARY.md
@D:\DTH\sandulieu\CLAUDE.md
@D:\DTH\sandulieu\app\(admin-portal)\admin\organizations\[organizationId]\departments\page.tsx
@D:\DTH\sandulieu\features\departments\department.query-hooks.ts
@D:\DTH\sandulieu\shared\api\member.api.ts

<interfaces>
Existing nested members route to link into:
```ts
/admin/organizations/[organizationId]/departments/[departmentId]/members
```

From features/departments/department.query-hooks.ts:

```ts
export function useDepartmentDetailQuery(
  organizationId: string,
  departmentId: string
);
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the department detail route with summary-oriented parent context</name>
  <files>app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx, features/departments/components/DepartmentDetailSummary.tsx</files>
  <behavior>
    - Test 1: detail route loads a department by stable `organizationId` + `departmentId`.
    - Test 2: summary shows id, code, name, status, and other contract-backed metadata.
    - Test 3: parent organization context remains visible in the detail header and actions.
    - Test 4: visible actions include back to scoped list and edit entrypoints, with delete wiring intentionally deferred to 03-05.
  </behavior>
  <action>Create the dedicated department detail page required by Phase 3. Keep the layout summary-oriented, not dashboard-heavy. Render the core department fields, explicit status badge, any cleanly supported metadata from 03-01, and visible organization-qualified navigation so the user never loses parent context. Do not introduce Phase 4 member CRUD changes on this screen.</action>
  <verify>
    <automated>pnpm exec eslint app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx features/departments/components/DepartmentDetailSummary.tsx</automated>
  </verify>
  <done>The detail route exists and gives users a summary-first department screen with visible parent organization context and back/edit actions, ready for shared delete wiring in 03-05.</done>
</task>

<task type="auto">
  <name>Task 2: Add related member summary and nested members navigation</name>
  <files>features/departments/components/DepartmentMemberSummary.tsx, app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx</files>
  <behavior>
    - Test 1: detail route shows member summary scoped to the same organizationId + departmentId.
    - Test 2: member summary displays count and optional preview when the contract provides it cleanly.
    - Test 3: member summary includes a clear CTA to the nested members route.
  </behavior>
  <action>Use the member-summary data produced in 03-01 to render read-only related-member context on the detail page. Include at minimum a count plus a clear route into `/admin/organizations/[organizationId]/departments/[departmentId]/members`. Keep this read-only and summary-oriented so it satisfies DEPT-06 without expanding into Phase 4 member editing work.</action>
  <verify>
    <automated>pnpm exec eslint features/departments/components/DepartmentMemberSummary.tsx app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx</automated>
  </verify>
  <done>The department detail screen shows related member summary and gives the user a clear path into the nested members surface.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                     | Description                                                         |
| -------------------------------------------- | ------------------------------------------------------------------- |
| detail route params → department detail page | Untrusted nested identity controls which record is viewed           |
| detail payload → summary rendering           | Data must stay scoped to the selected organizationId + departmentId |
| member-summary CTA → nested members surface  | Navigation must preserve hierarchy identity correctly               |

## STRIDE Threat Register

| Threat ID | Category | Component                 | Disposition | Mitigation Plan                                                                                                                     |
| --------- | -------- | ------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| T-03-14   | T        | department detail route   | mitigate    | Resolve the record only through stable `organizationId` + `departmentId` and render explicit loading/error states for invalid scope |
| T-03-15   | I        | detail summary page       | mitigate    | Restrict the page to department summary, parent context, and member-summary fields required by Phase 3                              |
| T-03-16   | D        | member-summary rendering  | mitigate    | Consume precomputed contract-backed summary data instead of running ad hoc list-wide transforms in the page                         |
| T-03-17   | E        | nested members navigation | mitigate    | Generate the members CTA from stable `organizationId` + `departmentId` route identity only                                          |

</threat_model>

<verification>
- `pnpm exec eslint app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/page.tsx features/departments/components/DepartmentDetailSummary.tsx features/departments/components/DepartmentMemberSummary.tsx`
- `pnpm build`
- Manual browser checks:
  - open `/admin/organizations/<organizationId>/departments/<departmentId>` and confirm summary + parent organization context
  - confirm member summary is visible
  - confirm CTA leads to `/admin/organizations/<organizationId>/departments/<departmentId>/members`
</verification>

<success_criteria>

- DEPT-06 is fully satisfied with a dedicated detail page and related member summary.
- Parent organization visibility is preserved on the detail route.
- The detail page is summary-oriented and does not drift into dashboard scope.
- The detail page establishes a clear navigation bridge into the members hierarchy.
  </success_criteria>

<output>
After completion, create `.planning/phases/03-department-management-in-organization-scope/03-department-management-in-organization-scope-04-SUMMARY.md`
</output>
