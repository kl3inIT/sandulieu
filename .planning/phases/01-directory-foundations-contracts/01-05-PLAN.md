---
phase: 01-directory-foundations-contracts
plan: 05
type: execute
wave: 5
depends_on:
  - 01
  - 02
  - 03
  - 04
files_modified:
  - features/organizations/organization.form.ts
  - features/organizations/components/OrganizationForm.tsx
  - features/departments/department.form.ts
  - features/members/member.form.ts
  - app/(admin-portal)/admin/organizations/page.tsx
  - app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx
  - app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/members/page.tsx
autonomous: true
requirements:
  - UX-02
  - UX-06
  - UX-07
  - ARCH-05
must_haves:
  truths:
    - "Create/edit forms for organizations, departments, and members share one explicit TanStack Form contract shape instead of page-specific validation logic."
    - "At least one real form consumer proves the exported form contract can drive TanStack Form validation and submission behavior end to end."
    - "Route-level proof pages actually parse URL/search params into the shared list-state helpers and pass normalized state into feature query options/hooks."
  artifacts:
    - path: "features/organizations/organization.form.ts"
      provides: "Organization TanStack Form-ready contract helpers"
      exports:
        [
          "organizationFormSchema",
          "getOrganizationFormDefaults",
          "createOrganizationFormOptions",
        ]
    - path: "features/departments/department.form.ts"
      provides: "Department TanStack Form-ready contract helpers"
      exports:
        [
          "departmentFormSchema",
          "getDepartmentFormDefaults",
          "createDepartmentFormOptions",
        ]
    - path: "features/members/member.form.ts"
      provides: "Member TanStack Form-ready contract helpers"
      exports:
        ["memberFormSchema", "getMemberFormDefaults", "createMemberFormOptions"]
    - path: "features/organizations/components/OrganizationForm.tsx"
      provides: "Minimal proof consumer using `useForm` with the organization form contract"
      exports: ["OrganizationForm"]
    - path: "app/(admin-portal)/admin/organizations/page.tsx"
      provides: "Route proof that converts search params into normalized organization list state"
      exports: ["default"]
  key_links:
    - from: "features/organizations/components/OrganizationForm.tsx"
      to: "features/organizations/organization.form.ts"
      via: "`useForm` validators/defaults/onSubmit contract"
      pattern: "useForm|createOrganizationFormOptions"
    - from: "app/(admin-portal)/admin/organizations/page.tsx"
      to: "features/directory/shared/directory-list-state.ts"
      via: "search-param parse/serialize into normalized list state"
      pattern: "parseDirectoryListState|serializeDirectoryListState"
    - from: "app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx"
      to: "features/directory/shared/directory-scope.ts"
      via: "route params normalized into organization scope before querying"
      pattern: "createOrganizationScope"
    - from: "app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/members/page.tsx"
      to: "features/directory/shared/directory-scope.ts"
      via: "route params normalized into department scope before querying"
      pattern: "createDepartmentScope"
---

<objective>
Add the Phase 1 proof layer for consistent TanStack Form usage and URL-owned route consumption so the shared foundations are demonstrably usable before full CRUD screens begin in later phases.

Purpose: Close the remaining Phase 1 gaps by proving both UX-06 and UX-02/UX-07 with real consumers rather than contract-only files.
Output: Entity form-contract modules, one minimal Organization form proof component, and three thin route proof pages that consume normalized URL state and parent-qualified query hooks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-directory-foundations-contracts/RESEARCH.md
@.planning/phases/01-directory-foundations-contracts/VALIDATION.md
@CLAUDE.md
@features/posts/components/PostForm.tsx
@app/(admin-portal)/admin/posts/page.tsx
@.planning/phases/01-directory-foundations-contracts/01-02-PLAN.md
@.planning/phases/01-directory-foundations-contracts/01-03-PLAN.md
@.planning/phases/01-directory-foundations-contracts/01-04-PLAN.md

<interfaces>
From features/posts/components/PostForm.tsx:
```ts
const form = useForm({
  defaultValues: initialValues ?? defaultValues,
  validators: {
    onChange: postFormSchema,
  },
  onSubmit: async ({ value }) => {
    const payload = postFormSchema.parse(value);
    await onSubmit(payload);
  },
});
```

From features/directory/shared/directory-list-state.ts (created in Plan 02):

```ts
export function parseDirectoryListState(...): DirectoryListState;
export function serializeDirectoryListState(state: DirectoryListState): URLSearchParams;
export function getDefaultDirectoryListState(...): DirectoryListState;
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TanStack Form-ready entity contracts and one proof consumer</name>
  <files>features/organizations/organization.form.ts, features/organizations/components/OrganizationForm.tsx, features/departments/department.form.ts, features/members/member.form.ts</files>
  <behavior>
    - Each entity exports Zod schema, typed defaults, and a TanStack Form-ready helper that returns validators/defaultValues/submit parsing shape.
    - The organization proof component uses `useForm` against the exported organization helper instead of hand-writing form wiring in the page.
    - Form submission parsing keeps stable ids and parent references explicit, never deriving identity from display labels.
  </behavior>
  <action>Implement UX-06 explicitly, not as schema-only placeholders. In each `*.form.ts`, export a TanStack Form-ready API such as `create<Entity>FormOptions` alongside schema and default-value builders so later create/edit screens can plug into `useForm` with consistent validation and submission behavior. Then create `features/organizations/components/OrganizationForm.tsx` as the minimal proof consumer that actually calls `useForm`, uses the exported helper, validates on change, parses on submit, and exposes a typed submit surface. This is the required proof that the Phase 1 form pattern is executable, not just described. Keep the UI thin and implementation-serving; do not build full create/edit screens or expand into custom design work.</action>
  <verify>
    <automated>pnpm exec eslint features/organizations/organization.form.ts features/organizations/components/OrganizationForm.tsx features/departments/department.form.ts features/members/member.form.ts && pnpm build</automated>
  </verify>
  <done>All three entity modules export TanStack Form-ready helpers, and at least the organization feature proves those helpers can drive a real `useForm` validation/submission path successfully through build validation.</done>
</task>

<task type="auto">
  <name>Task 2: Add thin route proof pages that normalize URL and route state before querying</name>
  <files>app/(admin-portal)/admin/organizations/page.tsx, app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx, app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/members/page.tsx</files>
  <action>Create minimal route proof pages whose only job is to demonstrate the intended orchestration path concretely. The organizations page must read search params, normalize them through `parseDirectoryListState` or `getDefaultDirectoryListState`, and pass the normalized object into the organization query-options/hooks and table-state helpers from Plans 02-03. The departments page must normalize `organizationId` from the route into `createOrganizationScope(...)`, combine that scope with normalized list state, and pass both into department query options/hooks from Plan 03. The members page must normalize `organizationId` plus `departmentId` into `createDepartmentScope(...)`, combine that scope with normalized list state, and pass the resulting object into member query options/hooks from Plan 04. Route params must use stable opaque `id` values, while business codes remain separate data fields per the resolved RESEARCH decision. Keep UI intentionally thin: enough markup to prove the data path, not later-phase enterprise table or CRUD UX.</action>
  <verify>
    <automated>pnpm exec eslint "app/(admin-portal)/admin/organizations/page.tsx" "app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx" "app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/members/page.tsx" && pnpm build</automated>
  </verify>
  <done>Each route proof page consumes normalized URL-owned list state and explicit parent scope before querying, so none of the pages can satisfy the plan by bypassing the shared directory helpers.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                        | Description                                                                                          |
| ----------------------------------------------- | ---------------------------------------------------------------------------------------------------- |
| route/search params → shared list/scope helpers | Untrusted route and query-string inputs must be normalized before driving query keys or table state. |
| form input → form helpers                       | User-entered values must be validated through Zod + TanStack Form before becoming feature payloads.  |
| proof pages → feature hooks                     | Pages must orchestrate only and must not rebuild query identity or validation logic locally.         |

## STRIDE Threat Register

| Threat ID | Category | Component                        | Disposition | Mitigation Plan                                                                                                                                            |
| --------- | -------- | -------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-17   | T        | route proof pages                | mitigate    | Parse search params into typed list-state defaults and convert route params into explicit scope helpers before querying.                                   |
| T-01-18   | I        | `features/*/*.form.ts`           | mitigate    | Validate and parse user input through Zod-backed TanStack Form helpers so form payloads retain explicit ids/parent references and reject malformed values. |
| T-01-19   | S        | route identity                   | mitigate    | Use stable opaque `id` route params only; do not treat display names or business codes as route identity in Phase 1 proof pages.                           |
| T-01-20   | D        | proof pages + build verification | accept      | Phase 1 proof pages are lightweight and mock-data backed; accept low performance exposure beyond build/lint validation.                                    |

</threat_model>

<verification>
- `pnpm exec eslint features/organizations/organization.form.ts features/organizations/components/OrganizationForm.tsx features/departments/department.form.ts features/members/member.form.ts "app/(admin-portal)/admin/organizations/page.tsx" "app/(admin-portal)/admin/organizations/[organizationId]/departments/page.tsx" "app/(admin-portal)/admin/organizations/[organizationId]/departments/[departmentId]/members/page.tsx"`
- `pnpm build`
- Confirm `OrganizationForm.tsx` uses `useForm` with the exported organization form helper rather than page-local validation wiring.
- Confirm each route proof page normalizes search params into shared list-state helpers and route params into scope helpers before calling feature queries.
</verification>

<success_criteria>

- UX-06 is executable because entity form modules export TanStack Form-ready helpers and at least one proof consumer demonstrates the pattern in real code.
- UX-02 and UX-07 are executable because route proof pages feed normalized URL-owned list state into feature query and table helpers.
- Route identity is explicit and backend-friendly: pages use stable opaque ids in params, while business codes remain separate fields in the domain model.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-directory-foundations-contracts/01-directory-foundations-contracts-05-SUMMARY.md`
</output>
