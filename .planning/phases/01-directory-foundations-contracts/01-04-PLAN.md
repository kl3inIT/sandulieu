---
phase: 01-directory-foundations-contracts
plan: 04
type: execute
wave: 4
depends_on:
  - 01
  - 02
  - 03
files_modified:
  - features/members/index.ts
  - features/members/member.types.ts
  - features/members/member.service.ts
  - features/members/member.query-options.ts
  - features/members/member.query-hooks.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-04
must_haves:
  truths:
    - "Members have their own feature module instead of sharing organization or department data logic."
    - "Member list and detail query surfaces always carry full organization-plus-department scope."
    - "Member cache identity cannot collide across departments because the full parent hierarchy is enforced at the query boundary."
  artifacts:
    - path: "features/members/member.service.ts"
      provides: "Member DTO-to-model mapping boundary with parent references preserved"
      exports: ["getMembers", "getMemberById"]
    - path: "features/members/member.query-options.ts"
      provides: "Member parent-qualified query options"
      exports: ["memberQueryKeys", "memberListQueryOptions", "memberDetailQueryOptions"]
    - path: "features/members/member.query-hooks.ts"
      provides: "Member hook layer wrapping parent-qualified query options"
      exports: ["useMemberListQuery", "useMemberDetailQuery"]
    - path: "features/members/index.ts"
      provides: "Member feature barrel exports"
      exports: ["memberQueryKeys", "memberListQueryOptions", "memberDetailQueryOptions"]
  key_links:
    - from: "features/members/member.service.ts"
      to: "shared/api/member.api.ts"
      via: "DTO-to-model mapping with parent references preserved"
      pattern: "map.*\(|get.*Api"
    - from: "features/members/member.query-options.ts"
      to: "features/directory/shared/directory-query-keys.ts"
      via: "structured list/detail cache keys"
      pattern: "createDirectoryListKey|createDirectoryDetailKey"
    - from: "features/members/member.query-options.ts"
      to: "features/directory/shared/directory-scope.ts"
      via: "organization + department qualified detail/list scope"
      pattern: "organizationId|departmentId"
---

<objective>
Create the member feature-module skeleton on top of the shared Phase 1 contracts so the deepest hierarchy seam exists before route-level proof work begins.

Purpose: Finish the Phase 1 feature-layer foundation by isolating member logic behind the same `shared/api -> feature service -> query options/hooks` layering, while requiring the full parent hierarchy at the query boundary.
Output: A separate member feature module with types, service mapping, query options, query hooks, and barrel exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-directory-foundations-contracts/RESEARCH.md
@.planning/phases/01-directory-foundations-contracts/VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@features/posts/index.ts
@features/posts/post.service.ts
@features/posts/post.query-options.ts
@features/posts/post.query-hooks.ts
@.planning/phases/01-directory-foundations-contracts/01-03-PLAN.md

<interfaces>
From features/posts/post.service.ts:
```ts
export async function getPosts(): Promise<PostsListResponse>;
export async function getPostById(postId: number): Promise<PostDetailResponse>;
export async function createPost(payload: CreatePostPayload): Promise<PostModel>;
export async function updatePost(postId: number, payload: UpdatePostPayload): Promise<PostModel>;
```

From features/posts/post.query-options.ts:

```ts
export const postQueryKeys = {
  all: ["posts"] as const,
  lists: () => [...postQueryKeys.all, "list"] as const,
  list: () => [...postQueryKeys.lists()] as const,
  details: () => [...postQueryKeys.all, "detail"] as const,
  detail: (postId: number) => [...postQueryKeys.details(), postId] as const,
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the member feature seam with full parent-qualified query identity</name>
  <files>features/members/index.ts, features/members/member.types.ts, features/members/member.service.ts, features/members/member.query-options.ts, features/members/member.query-hooks.ts</files>
  <behavior>
    - Member list/detail query options require both `organizationId` and `departmentId` in scope.
    - Member services map raw API DTOs into shared models with both parent references preserved.
    - Member query keys include parent scope and params so nested caches cannot collide across routes.
  </behavior>
  <action>Create the member feature module using the same layering pattern as organizations and departments, but require the full organization-plus-department scope at the query boundary because members live under the full parent hierarchy in Phase 1 contracts. Preserve stable route identity by keying detail lookups with scope objects plus the member `id`, not display fields or business labels. Keep the module foundation-focused: list/detail query support and invalidation-ready hook seams only. Do not add page orchestration or form proof work here; that remains in the downstream proof plan.</action>
  <verify>
    <automated>pnpm exec eslint features/members/index.ts features/members/member.types.ts features/members/member.service.ts features/members/member.query-options.ts features/members/member.query-hooks.ts</automated>
  </verify>
  <done>The member feature module exists, preserves repo layering, and all member query surfaces require full parent-qualified scope.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                          | Description                                                                                                      |
| ------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| shared API DTOs → member feature services         | Wire-format data must be normalized before page/UI consumers use it.                                             |
| route params/scope helpers → member query options | Untrusted parent/child identifiers must be validated into explicit scope objects before becoming cache identity. |
| member hooks → future pages                       | Pages must not bypass the feature boundary and import raw member API modules directly.                           |

## STRIDE Threat Register

| Threat ID | Category | Component                                  | Disposition | Mitigation Plan                                                                                                                                               |
| --------- | -------- | ------------------------------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-13   | T        | `features/members/member.service.ts`       | mitigate    | Keep DTO-to-model mapping in the member service file so pages/components never trust raw API shapes directly.                                                 |
| T-01-14   | I        | `features/members/member.query-options.ts` | mitigate    | Build list/detail keys from explicit organization and department scope helpers so member hooks cannot fetch or cache data outside the current parent context. |
| T-01-15   | S        | member query identity                      | mitigate    | Use stable opaque `id` fields and explicit parent scope only; never key member queries or routes by mutable display names.                                    |
| T-01-16   | D        | member list queries                        | accept      | Phase 1 uses lightweight mock reads only; accept low denial-of-service exposure beyond bounded pagination already enforced by shared contracts.               |

</threat_model>

<verification>
- `pnpm exec eslint features/members/index.ts features/members/member.types.ts features/members/member.service.ts features/members/member.query-options.ts features/members/member.query-hooks.ts`
- Confirm no route/page code is needed to consume the member module.
- Confirm member keys require both `organizationId` and `departmentId` in every list/detail path.
</verification>

<success_criteria>

- Members have a separate feature module aligned with the repo’s existing posts layering.
- The member service owns DTO-to-model mapping instead of leaking wire DTOs into pages.
- Full parent-qualified query identity is explicit for members before any route-level proof is added.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-directory-foundations-contracts/01-directory-foundations-contracts-04-SUMMARY.md`
</output>
