---
phase: 01-directory-foundations-contracts
plan: 03
type: execute
wave: 3
depends_on:
  - 01
  - 02
files_modified:
  - features/organizations/index.ts
  - features/organizations/organization.types.ts
  - features/organizations/organization.service.ts
  - features/organizations/organization.query-options.ts
  - features/organizations/organization.query-hooks.ts
  - features/departments/index.ts
  - features/departments/department.types.ts
  - features/departments/department.service.ts
  - features/departments/department.query-options.ts
  - features/departments/department.query-hooks.ts
autonomous: true
requirements:
  - ARCH-01
  - ARCH-02
  - ARCH-04
must_haves:
  truths:
    - "Organizations and departments each have their own feature module instead of collapsing data and page logic together."
    - "Organization query surfaces use shared contracts, and department query surfaces remain explicitly qualified by organization scope."
    - "Department list and detail queries cannot collide across organizations because parent-qualified cache identity is enforced at the feature boundary."
  artifacts:
    - path: "features/organizations/organization.service.ts"
      provides: "Organization DTO-to-model mapping boundary"
      exports: ["getOrganizations", "getOrganizationById"]
    - path: "features/organizations/organization.query-options.ts"
      provides: "Organization query-key factory and queryOptions builders"
      exports: ["organizationQueryKeys", "organizationListQueryOptions", "organizationDetailQueryOptions"]
    - path: "features/departments/department.query-options.ts"
      provides: "Department parent-qualified query options"
      exports: ["departmentQueryKeys", "departmentListQueryOptions", "departmentDetailQueryOptions"]
    - path: "features/departments/department.query-hooks.ts"
      provides: "Department hook layer wrapping parent-qualified query options"
      exports: ["useDepartmentListQuery", "useDepartmentDetailQuery"]
  key_links:
    - from: "features/organizations/organization.service.ts"
      to: "shared/api/organization.api.ts"
      via: "DTO-to-model mapping"
      pattern: "map.*\(|get.*Api"
    - from: "features/departments/department.service.ts"
      to: "shared/api/department.api.ts"
      via: "DTO-to-model mapping with organization-qualified access"
      pattern: "map.*\(|get.*Api"
    - from: "features/organizations/organization.query-options.ts"
      to: "features/directory/shared/directory-query-keys.ts"
      via: "structured list/detail cache keys"
      pattern: "createDirectoryListKey|createDirectoryDetailKey"
    - from: "features/departments/department.query-options.ts"
      to: "features/directory/shared/directory-scope.ts"
      via: "organization-qualified detail/list scope"
      pattern: "organizationId"
---

<objective>
Create the organization and department feature-module skeletons on top of the shared Phase 1 contracts so later CRUD phases extend real seams instead of inventing module structure during implementation.

Purpose: Satisfy ARCH-01 and ARCH-02 early by proving the directory domain can follow the repo’s established `shared/api -> feature service -> query options/hooks -> pages/components` layering for the first two hierarchy levels.
Output: Separate organization and department feature modules with types, service mapping, query options, query hooks, and barrel exports.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-directory-foundations-contracts/RESEARCH.md
@.planning/phases/01-directory-foundations-contracts/VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@CLAUDE.md
@features/posts/index.ts
@features/posts/post.service.ts
@features/posts/post.query-options.ts
@features/posts/post.query-hooks.ts

<interfaces>
From features/posts/post.service.ts:
```ts
export async function getPosts(): Promise<PostsListResponse>;
export async function getPostById(postId: number): Promise<PostDetailResponse>;
export async function createPost(payload: CreatePostPayload): Promise<PostModel>;
export async function updatePost(postId: number, payload: UpdatePostPayload): Promise<PostModel>;
```

From features/posts/post.query-options.ts:

```ts
export const postQueryKeys = {
  all: ["posts"] as const,
  lists: () => [...postQueryKeys.all, "list"] as const,
  list: () => [...postQueryKeys.lists()] as const,
  details: () => [...postQueryKeys.all, "detail"] as const,
  detail: (postId: number) => [...postQueryKeys.details(), postId] as const,
};
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the organization feature seam with shared contract-based query identity</name>
  <files>features/organizations/index.ts, features/organizations/organization.types.ts, features/organizations/organization.service.ts, features/organizations/organization.query-options.ts, features/organizations/organization.query-hooks.ts</files>
  <behavior>
    - Organization list/detail query options use shared directory contracts and expose stable query keys.
    - Organization services map raw API DTOs into shared UI models instead of exposing wire DTOs to pages.
    - Organization hooks wrap query options using the existing posts feature pattern so later invalidation seams stay in the feature layer.
  </behavior>
  <action>Create the organization module per ARCH-01, following the posts feature shape while adapting it to the directory contracts from Plans 01-02. The service file must consume `shared/api/organization.api.ts` and map DTOs into shared organization models. The query-options file must consume the shared directory query-key helpers from Plan 02 so organization list/detail identity stays structured and future-safe. The query-hooks file should wrap the query options and preserve the repo pattern for later invalidation seams. Keep pages/components out of this plan.</action>
  <verify>
    <automated>pnpm exec eslint features/organizations/index.ts features/organizations/organization.types.ts features/organizations/organization.service.ts features/organizations/organization.query-options.ts features/organizations/organization.query-hooks.ts</automated>
  </verify>
  <done>The organization feature module exists, preserves repo layering, and exposes list/detail query surfaces built from shared contracts instead of page-local logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the department feature seam with mandatory organization-qualified scope</name>
  <files>features/departments/index.ts, features/departments/department.types.ts, features/departments/department.service.ts, features/departments/department.query-options.ts, features/departments/department.query-hooks.ts</files>
  <behavior>
    - Department list/detail query options require organization-qualified scope even when resolving a single department.
    - Department services map raw API DTOs into shared models instead of exposing wire DTOs to pages.
    - Department query keys include organization scope so nested caches do not collide across parent contexts.
  </behavior>
  <action>Create the department module as the second hierarchy seam, again following the posts layering while enforcing the resolved Phase 1 uniqueness decision that department identity is only guaranteed inside organization scope. Services must consume the raw department API module from Plan 01 and map DTOs into shared models. Query-option files must consume shared directory query-key and scope helpers from Plan 02 so departments are always qualified by `organizationId` plus stable child `id`. Query hooks should wrap those query options and keep mutation/invalidation seams in the feature layer for later phases.</action>
  <verify>
    <automated>pnpm exec eslint features/departments/index.ts features/departments/department.types.ts features/departments/department.service.ts features/departments/department.query-options.ts features/departments/department.query-hooks.ts</automated>
  </verify>
  <done>The department feature module exists, preserves repo layering, and departments cannot be queried or keyed without explicit organization scope.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                                           | Description                                                                                                      |
| -------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| shared API DTOs → feature services                 | Wire-format data must be normalized before page/UI consumers use it.                                             |
| route params/scope helpers → feature query options | Untrusted parent/child identifiers must be validated into explicit scope objects before becoming cache identity. |
| feature hooks → future pages                       | Pages must not bypass feature boundaries and import raw API modules directly.                                    |

## STRIDE Threat Register

| Threat ID | Category | Component                                                                                      | Disposition | Mitigation Plan                                                                                                                                         |
| --------- | -------- | ---------------------------------------------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-01-09   | T        | `features/organizations/organization.service.ts`, `features/departments/department.service.ts` | mitigate    | Keep DTO-to-model mapping in service files so pages/components never trust raw API shapes directly.                                                     |
| T-01-10   | I        | `features/departments/department.query-options.ts`                                             | mitigate    | Build list/detail keys from explicit organization scope helpers so nested resource hooks cannot fetch or cache data outside the current parent context. |
| T-01-11   | S        | organization and department query identity                                                     | mitigate    | Use stable opaque `id` fields and explicit parent scope only; never key queries or routes by mutable display names.                                     |
| T-01-12   | D        | organization and department list queries                                                       | accept      | Phase 1 uses lightweight mock reads only; accept low denial-of-service exposure beyond bounded pagination already enforced by shared contracts.         |

</threat_model>

<verification>
- `pnpm exec eslint features/organizations/index.ts features/organizations/organization.types.ts features/organizations/organization.service.ts features/organizations/organization.query-options.ts features/organizations/organization.query-hooks.ts features/departments/index.ts features/departments/department.types.ts features/departments/department.service.ts features/departments/department.query-options.ts features/departments/department.query-hooks.ts`
- Confirm no feature module imports raw API files from route pages directly.
- Confirm department keys require `organizationId` in every list/detail path.
</verification>

<success_criteria>

- Organizations and departments each have a separate feature module aligned with the repo’s existing posts layering.
- Service files own DTO-to-model mapping instead of leaking wire DTOs into pages.
- Parent-qualified query identity is explicit for departments before any route-level proof is added.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-directory-foundations-contracts/01-directory-foundations-contracts-03-SUMMARY.md`
</output>
