---
phase: 01-directory-foundations-contracts
plan: 06
type: execute
wave: 6
depends_on:
  - 05
files_modified:
  - app/(admin-portal)/admin/organizations/page.tsx
autonomous: true
requirements:
  - UX-06
gap_closure: true
must_haves:
  truths:
    - "Admin user sees a visible, discoverable entrypoint to the organization form proof from `/admin/organizations`."
    - "Admin user can reach and use the organization form proof without manually typing a hidden URL or editing code."
    - "The organization form proof renders inside the admin organizations proof experience and its validate-on-change plus submit flow remains runnable without runtime errors."
  artifacts:
    - path: "app/(admin-portal)/admin/organizations/page.tsx"
      provides: "Visible form-proof CTA plus mounted organization form proof within the admin organizations proof page"
      exports: ["default"]
  key_links:
    - from: "app/(admin-portal)/admin/organizations/page.tsx"
      to: "features/organizations/components/OrganizationForm.tsx"
      via: "page-level import and render inside a labeled proof section"
      pattern: "OrganizationForm"
    - from: "app/(admin-portal)/admin/organizations/page.tsx"
      to: "#organization-form-proof"
      via: "visible CTA or anchor link from the page header"
      pattern: 'href="#organization-form-proof"|href={''#organization-form-proof''}'
    - from: "features/organizations/components/OrganizationForm.tsx"
      to: "app/(admin-portal)/admin/organizations/page.tsx"
      via: "typed onSubmit callback used for proof success feedback"
      pattern: "onSubmit"
---

<objective>
Close the diagnosed Phase 1 UAT gap by making the existing organization form proof reachable and testable from the admin organizations proof experience.

Purpose: The current form proof already exists, but users cannot discover it from `/admin/organizations`, so UX-06 cannot be verified in UAT.
Output: A visible form-proof entrypoint and rendered organization form proof on the admin organizations proof page.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-directory-foundations-contracts/01-UAT.md
@.planning/phases/01-directory-foundations-contracts/01-directory-foundations-contracts-05-SUMMARY.md
@app/(admin-portal)/admin/organizations/page.tsx
@features/organizations/components/OrganizationForm.tsx

<interfaces>
From features/organizations/components/OrganizationForm.tsx:
```ts
export function OrganizationForm({
  mode,
  initialValues,
  submitLabel,
  isPending = false,
  onSubmit,
  onReset,
}: OrganizationFormProps)
```

From app/(admin-portal)/admin/organizations/page.tsx:

```ts
export default function AdminOrganizationsPage();
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make the organization form proof discoverable on the admin organizations page</name>
  <files>app/(admin-portal)/admin/organizations/page.tsx</files>
  <action>Update the existing admin organizations proof page to close the exact UAT gap recorded in `01-UAT.md`: the form proof exists but has no discoverable user path. Recommended solution: embed the existing `OrganizationForm` directly on `/admin/organizations` and add a visible CTA that jumps to that proof section. Choose this over a new dedicated route because it fixes the reported issue with the smallest change surface, keeps the proof in the same admin experience the user already opened, and avoids creating extra navigation or route files for Phase 1. Import `OrganizationForm`, add a clearly labeled proof section with a stable anchor such as `organization-form-proof`, and place a visible header action or button linking to that section. Provide a minimal local `onSubmit` proof handler that resolves successfully and shows lightweight success feedback or submitted values so users can exercise validate-on-change and submit behavior without introducing later-phase CRUD persistence. Preserve the existing organization list proof, URL normalization flow, and department links; do not add extra design work or expand into a full create/edit screen.</action>
  <verify>
    <automated>pnpm exec eslint "app/(admin-portal)/admin/organizations/page.tsx" && pnpm build</automated>
  </verify>
  <done>`/admin/organizations` visibly exposes the organization form proof, users can reach it from an on-page CTA, and the form can be filled and submitted from that page without runtime errors while existing list proof behavior remains intact.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                               | Description                                                                                                                      |
| -------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| page CTA → proof section               | User navigation inside the page must reach a real mounted proof target, not dead text or hidden UI.                              |
| form input → proof submit handler      | Untrusted user input must continue flowing through the existing TanStack Form + Zod validation before success feedback is shown. |
| existing list proof → added form proof | The gap fix must not break the current URL-owned organization list proof or department navigation links.                         |

## STRIDE Threat Register

| Threat ID | Category | Component                                         | Disposition | Mitigation Plan                                                                                                        |
| --------- | -------- | ------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------- |
| T-01-21   | T        | `app/(admin-portal)/admin/organizations/page.tsx` | mitigate    | Reuse the existing `OrganizationForm` validation path instead of duplicating raw input handling in the page.           |
| T-01-22   | D        | `/admin/organizations` proof page                 | mitigate    | Keep the change to a single route file and preserve the existing list-query/render path while adding the form section. |
| T-01-23   | R        | proof submit feedback                             | accept      | This is Phase 1 proof UX backed by local success feedback, so audit-grade submission evidence is not required yet.     |

</threat_model>

<verification>
- `pnpm exec eslint "app/(admin-portal)/admin/organizations/page.tsx"`
- `pnpm build`
- Open `/admin/organizations` and confirm there is a visible CTA leading to the organization form proof section.
- Confirm the proof section renders `OrganizationForm`, validates on change, and accepts a valid submit without runtime errors.
- Confirm the existing organization list proof cards and department links still render on the same page.
</verification>

<success_criteria>

- The exact diagnosed UAT complaint, "i dont see button to go to form," is no longer true on `/admin/organizations`.
- The organization form proof is reachable from a visible admin-page path and testable in the same experience.
- The fix stays tightly scoped to discoverability and proof reachability, with no unrelated Phase 2 CRUD expansion.
  </success_criteria>

<output>
After completion, create `.planning/phases/01-directory-foundations-contracts/01-directory-foundations-contracts-06-SUMMARY.md`
</output>
